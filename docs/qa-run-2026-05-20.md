# QA Run Log — 2026-05-20

Scope executed per request: sections **B, C, D, F, G, H, I** from `production/TEST_PLAN.md`.

## Environment
- Runner: Codex CLI in non-interactive container.
- Limitation: No live Wix/App Script deployment URL or browser DevTools session attached.
- Method used:
  1) Static checklist traceability against current source (`production/jys_quan_ly_nhan_su.html`, `production/Code.gs`).
  2) Security grep validations from AGENTS instructions.

## Result summary
- PASS (source-confirmed): 11
- FAIL (source-indicated defect): 2
- BLOCKED (runtime/browser required): 28

## Section B — Đăng nhập sai
- [BLOCKED] All runtime UX items require interactive browser + backend response validation.

## Section C — Đăng nhập quản lý
- [BLOCKED] Requires live Script Properties MANAGER_CODE and actual login flow.

## Section D — CRUD nhân viên
- [PASS] Branch options are exactly `Đô Lương`, `Vinh`, `Quảng Sơn` in form + filters.
- [PASS] PIN modal is shown only after create/regenerate path (guarded by `res.pinModal`) and not for normal update.
- [FAIL] **Employee PIN field is editable in edit mode** (input missing readonly/disabled).
  - Exact step: D → “Field Mã cá nhân trong form sửa là readonly (không gõ được)”.
  - Expected: input cannot be typed/edited.
  - Actual: rendered as `<input type="text" id="hrPin" maxlength="4" ...>` without readonly/disabled.
  - Console/network evidence: static source evidence (no browser capture available in this run).
  - Suspected layer: **Frontend** (`production/jys_quan_ly_nhan_su.html`).

## Section F — Ghi nhận vi phạm
- [PASS] ViPham records include/persist `ts` (frontend sends `Date.now()`, backend enforces fallback `Number(rec.ts)||Date.now()`).
- [BLOCKED] Amount math/toast/table/filter/export/delete require runtime flow and sheet verification.

## Section G — Đăng nhập nhân viên & quyền
- [PASS] `loginManager` and `loginStaff` are sent via POST (`call({body:...})`).
- [FAIL] `getMine` is currently sent via GET query params (contains `auth` and `empId` in URL).
  - Exact step: G → “authenticated actions (loginManager, loginStaff, getAll, getMine) appear as POST”.
  - Expected: `getMine` should be POST like other authenticated actions.
  - Actual: `call({params:{action:"getMine",empId:SESSION.empId,auth:SESSION.auth}})` triggers GET.
  - Console/network evidence: static source evidence; inferred network method from call path.
  - Suspected layer: **Frontend** (`production/jys_quan_ly_nhan_su.html`), with backend already supporting POST `getMine` action.
- [PASS] Backend POST router already includes `getMine` branch.
- [BLOCKED] Remaining visibility/permission payload assertions require live backend responses + DevTools.

## Section H — Kiểm tra (Quiz)
- [PASS] Quiz submit includes `name/branch/pos` in frontend payload and backend fallback backfills from employee record if missing.
- [PASS] KiemTra records include/persist `ts` (frontend sends `Date.now()`, backend enforces fallback `Number(rec.ts)||Date.now()`).
- [BLOCKED] Shuffle behavior, UX flows, delete propagation to sheet require runtime verification.

## Section I — Lỗi mạng / backend
- [BLOCKED] Offline/network toggle, Script Properties MANAGER_CODE removal, and AuditLog event verification require live Apps Script + spreadsheet.

## AGENTS security grep validation
Commands run:
- `grep -RIn --exclude-dir=.git -E 'script\.google\.com/macros/s/[A-Za-z0-9_-]{20,}' . || true`
- `grep -RIn --exclude-dir=.git '<old-shared-manager-code>' . || true`
- `grep -RIn --exclude-dir=.git 'Mã demo\|Demo:' production || true`

Observed:
- No committed real Apps Script `/macros/s/...` URL.
- No placeholder old shared manager code string.
- One hit for "Demo:" in a quiz answer option text in production HTML (`Demo: fallback workflows` not auth credential).

## Evidence references (snippets)
- `production/jys_quan_ly_nhan_su.html`:
  - API call helper uses POST for `body` and GET for `params`.
  - Manager/staff login uses POST body.
  - `getMine` currently uses `params` path (GET).
  - Employee PIN edit input lacks readonly/disabled.
  - ViPham/KiemTra payload include `ts: Date.now()`.
  - KiemTra payload includes `name/branch/pos`.
- `production/Code.gs`:
  - `doPost` routes include `loginManager`, `loginStaff`, `getAll`, `getMine`.
  - `saveViPham_` and `saveKiemTra_` enforce ts fallback.
  - `saveKiemTra_` backfills `name/branch/pos` from employee record.

